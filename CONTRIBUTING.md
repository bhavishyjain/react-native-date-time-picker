# Contributing

## Setup

```bash
npm install
npm run build
npm test
```

## Example app

```bash
cd example
npm install
npx expo start -c
```

## Guidelines

- Keep TypeScript strict
- Prefer native `Date` and `Intl` APIs over external date libraries
- Use Reanimated for animations and gesture-handler for gestures
